# Examples

`wechat-compatible.html` is a deliberately privacy-safe example showing the two key ideas:

1. Use WeChat CDN image URLs (`mmbiz.qpic.cn`).
2. Put critical layout rules directly in inline `style=""` attributes.

The image URLs are placeholders and must be replaced with your own WeChat-hosted image URLs.
